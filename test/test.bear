int fibo(int count){
    list li = {0,1};
    int result;
    if(count == 1){
        result = 0;
    }
    if(count == 2){
        result = 1;
    }
    int curIndex = 1;
    while(curIndex < count-1){
        li[curIndex+1] = li[curIndex] + li[curIndex-1];
        result = li[curIndex+1];
        curIndex = curIndex + 1;
    }
    return result;
}
int main()
{
int a=7;
list b = { 1,2,3,{4}};
<<(-1 + a + b[3][0]);
return fibo(10);
}